

Mapping b/w a protocol (ip) address and a hardware (mac) address is called

Address Resolution

---

when arp searches ip address in hardware address, it is tough, therefore we use hasing and direct indexing

Direct indexing and Hashing

---

command

```arp
arp -a
```

